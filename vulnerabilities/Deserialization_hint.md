# Deserialization Hints

- Use Python to craft a pickle and base64-encode it for testing.
- Start with safe objects like dicts to confirm the endpoint works, then experiment in a controlled environment.
- Never unpickle untrusted data in production.
