# Path Traversal Hints

- Use `..` to move up directories, e.g., `../README.md`.
- Percent-encode slashes if needed in certain clients.
- Combine with knowledge of repo layout to fetch interesting files under `/app`.
